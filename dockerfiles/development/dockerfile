# =====================
# Autogoal base image
# ---------------------

FROM autogoal/autogoal:base

USER root

ARG contrib

COPY dockerfiles/development/install-contrib.sh install-contrib.sh 
RUN ./install-contrib.sh $contrib

SHELL ["/bin/bash", "-c"]

USER coder

CMD [ "/bin/bash" ]

